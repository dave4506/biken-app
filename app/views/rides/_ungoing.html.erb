<%= form_for(current_user.active_relationships.find_by(ride_id: ride.id),
           html: { method: :delete },
           remote: true) do |f| %>
    <%= f.submit "Na", class: "btn btn-lg" %><br>
<% end %>
<%= render :partial => 'layouts/ridegoing', :locals => { :attendees => ride.attendees } %>
